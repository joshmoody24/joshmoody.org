---
interface Props {
  number: number;
}

const { number } = Astro.props;
---

&#8288;<sup id={`footnote-${number}-ref`}><button
  type="button"
  id={`footnote-${number}-btn`}
  style={`anchor-name: --footnote-${number}`}
  aria-label={`Show footnote ${number}`}
  aria-controls={`footnote-${number}`}
  popovertarget={`footnote-${number}`}
  popovertargetaction="toggle"
>{number}</button></sup>

<style>
  sup {
    color: var(--color-primary);
    vertical-align: super;
    line-height: 0;
    font-size: 0.833rem;
    font-weight: normal;
    font-style: normal;
    white-space: nowrap;
  }

  button {
    all: unset;
    cursor: pointer;
    text-decoration: underline;
  }
</style>
